VITE_API_URL=http://localhost:5299
VITE_USE_MOCKS=false
# Auth: empty = use Vite proxy /api-auth → test-apiconnect.febtw.co
# VITE_AUTH_URL=/api-auth/auth/Authentication
# Production needs a same-origin reverse proxy or CORS on the API.
