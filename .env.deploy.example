# Copia a .env.deploy (ignorado por git) para deploy local:
#   cp .env.deploy.example .env.deploy
#
# Luego: npm run deploy

DEPLOY_HOST=premiundev.com
DEPLOY_USER=dev
DEPLOY_PASSWORD=
DEPLOY_PATH=/var/www/premiundev
DEPLOY_PORT=22
